// const average = 8
// const message = average >= 7 ? "Aprovado" : "Reprovado"
// console.log("message", message)
const age = 10
const casa = false

// const isAble = message === "Aprovado" && age >= 18 && hasCar

// const message2 = isAble ? "Já pode tirar carteira" : "Não pode tirar carteira"
// console.log("message2", message2)

const test = age >= 18 || hasCar
console.log("test", test)

if (casa === false) {
  console.log("nao tem carro")
}

if (!hasCar) {
  console.log("nao tem carro")
}
